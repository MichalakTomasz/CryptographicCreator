<UserControl x:Class="ModuleREARegion.Views.ViewA"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ModuleREARegion.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True" >
    <Grid Grid.Row="1">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <GroupBox Grid.Row="0" Header="Public Key">
            <TextBlock Text="{Binding PrivateAndPublicKey}"/>
        </GroupBox>
        <GroupBox Grid.Row="1" Header="Private Key">
            <TextBlock Text="{Binding PrivateKey}"/>
        </GroupBox>
        <GroupBox Grid.Row="2" Header="Text ">
            <TextBox Text="{Binding Text}"/>
        </GroupBox>
        <GroupBox Grid.Row="3" Header="Decrypted text">
            <TextBlock Text="{Binding DectyptedText}"/>
        </GroupBox>
    </Grid>
</UserControl>
